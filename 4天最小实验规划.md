# 4天最小实验规划

## 1. 实验目标

本实验用于验证运输减碳报告中的模型升级方向是否值得继续投入。实验不改动现有企业原始数据，只在当前目录中读取 `订单数据.xlsx`，并使用已下载的公开 EVRP 数据集做模型扩展验证。

实验回答两个问题：

1. 企业真实运输数据中，加入运输时间后，CVRP 路线优化结果是否比单纯最短距离更有解释力。
2. 公开 EVRP 数据中，加入电池、能耗和充电站约束后，新能源路径模型是否能够稳定得到可行路线。

本轮实验只做最小验证。企业数据和公开 benchmark 分开使用，不合并成一个数据集。

## 2. 数据范围

### 2.1 企业数据

文件：

```text
订单数据.xlsx
```

已检查到的数据结构：

| 数据表 | 规模 | 用途 |
|---|---:|---|
| 订单数据 | 12,098 单 | 构造货量、体积、件数和发收城市需求 |
| 运单数据 | 36,329 行 | 构造车辆、车型、运输段、里程和时间 |
| 车辆 | 5,868 辆 | 统计现有车辆使用结构 |
| 车型 | 68 类 | 后续车型匹配分析基础 |
| 封车网点 | 542 个 | 构造运输起点集合 |
| 解封网点 | 2,394 个 | 构造运输终点集合 |
| 估算物理运输段 | 13,549 段 | 作为路线优化的基础运输单元 |

关键口径：

运单数据中多行可能对应同一辆车、同一封车时间、同一路段承载多个运单。实验需要先聚合为物理运输段，再计算趟次、里程和时间。

物理运输段建议按以下字段聚合：

```text
vehicle_number
vehicle_type_name
实际行驶里程（km）
departure_time
arrival_time
departure_site_name
arrival_site_name
```

### 2.2 主公开数据

路径：

```text
external_data/e-cvrp_benchmark_instances
```

用途：

验证 EVRP 模型是否能处理新能源车辆的续航、能耗和充电站约束。

优先使用小规模实例：

```text
E-n29-k4-s7.evrp
E-n60-k5-s9.evrp
```

### 2.3 备用公开数据

路径：

```text
external_data/EVRP-TW-SPD-HMA
```

用途：

备用验证时间窗、部分充电和取送货约束。4天版本不作为主实验使用。

## 3. 实验设计

### 3.1 实验 A：企业数据 CVRP 与 Time-CVRP 对比

目标：

验证运输时间是否应该进入现有 CVRP 框架。

输入：

| 输入 | 构造方式 |
|---|---|
| 节点 | 从高频封车网点和解封网点中选择子网络 |
| 距离矩阵 | 按 OD 聚合实际行驶里程均值 |
| 时间矩阵 | 按 OD 聚合实际运输耗时均值 |
| 需求 | 按订单重量、体积、件数聚合 |
| 车辆容量 | 第一版使用统一容量，后续再细分车型 |

模型 1：Baseline CVRP

```text
目标：最小化总行驶距离
约束：每个需求点被服务，车辆容量不超限
```

模型 2：Time-CVRP

```text
目标：最小化 α × 标准化距离 + (1 - α) × 标准化时间
约束：沿用 Baseline CVRP
```

第一版参数：

```text
α = 0.5
```

敏感性参数：

```text
α = 0.3
α = 0.7
```

建议先选择高频子网络，避免全网过稀导致实验时间失控。优先选择上海、苏州、昆山、松江相关高频运输段。

### 3.2 实验 B：公开数据 CVRP 与 EVRP 对比

目标：

验证新能源车辆路径模型是否能在电池容量、单位能耗和充电站约束下生成可行路径。

输入：

| 输入 | 来源 |
|---|---|
| 客户节点 | `.evrp` 实例 |
| 仓库节点 | `.evrp` 实例 |
| 充电站 | `.evrp` 实例 |
| 客户需求 | `.evrp` 实例 |
| 车辆容量 | `.evrp` 实例 |
| 电池容量 | `.evrp` 实例 |
| 单位能耗 | `.evrp` 实例 |

模型 1：CVRP

```text
忽略电池和充电站，只考虑容量与距离。
```

模型 2：EVRP

```text
加入电池容量、单位能耗和充电站访问约束。
车辆行驶过程中电量不得低于 0。
车辆可在充电站恢复电量。
路线最终返回仓库。
```

本实验的重点是可行性和路径变化，不追求大规模最优。

## 4. 4天执行安排

### Day 1：数据口径与中间表

任务：

1. 读取 `订单数据.xlsx`。
2. 将运单行聚合为物理运输段。
3. 构造 OD 距离矩阵。
4. 构造 OD 时间矩阵。
5. 选择一个高频子网络。
6. 输出实验中间表。

产出：

```text
processed/physical_legs.csv
processed/od_distance.csv
processed/od_time.csv
processed/experiment_nodes.csv
```

检查点：

1. 物理运输段数量是否明显小于运单行数。
2. 高频子网络是否有足够 OD 记录。
3. 距离和时间是否存在明显异常值。

### Day 2：企业数据路线优化

任务：

1. 跑 Baseline CVRP。
2. 跑 Time-CVRP。
3. 比较不同 `α` 下的结果。
4. 输出路线结果和指标表。

产出：

```text
results/company_baseline_routes.csv
results/company_time_routes.csv
results/company_metrics.csv
```

核心指标：

| 指标 | 含义 |
|---|---|
| 总里程 | 检查路线距离变化 |
| 总运输时间 | 检查时间目标是否有效 |
| 趟次数 | 检查运输组织压缩程度 |
| 平均承载运单数 | 检查并单效果 |
| 估算碳排 | 用于减碳结果比较 |

### Day 3：公开 EVRP 验证

任务：

1. 解析 `.evrp` 文件。
2. 先跑 CVRP。
3. 再跑 EVRP。
4. 记录充电站访问、总距离、能耗和可行性。

产出：

```text
results/evrp_parsed_instances.csv
results/evrp_routes.csv
results/evrp_metrics.csv
```

核心指标：

| 指标 | 含义 |
|---|---|
| 是否可行 | 路线是否满足容量和电池约束 |
| 总距离 | 比较 CVRP 与 EVRP 路径变化 |
| 充电站访问次数 | 观察补能约束对路径的影响 |
| 最低剩余电量 | 判断续航约束是否紧张 |
| 总能耗 | 支撑新能源车辆路径分析 |

### Day 4：结果汇总与继续判断

任务：

1. 汇总企业数据实验结果。
2. 汇总公开 EVRP 实验结果。
3. 判断是否继续扩展到车型匹配和补能设施分析。
4. 形成报告可用的实验结论。

产出：

```text
results/four_day_experiment_summary.md
results/company_metrics_table.csv
results/evrp_metrics_table.csv
```

建议图表：

1. CVRP 与 Time-CVRP 指标对比表。
2. 不同 `α` 下总里程和总时间变化图。
3. EVRP 路线中充电站访问示意图。
4. CVRP 与 EVRP 的总距离、能耗、可行性对比表。

## 5. 判断标准

### 5.1 支持继续扩展的结果

出现以下结果时，建议继续做车型匹配和补能设施分析：

1. Time-CVRP 使总运输时间下降 8% 以上，同时总里程增加不超过 5%。
2. 不同 `α` 会产生明显不同的路径结构，说明时间目标确实影响路线选择。
3. EVRP 能稳定得到满足电池和充电站约束的可行路径。
4. EVRP 相比 CVRP 出现清晰的绕行、补能或路径重组现象。
5. 企业数据中存在高频、稳定、可解释的运输子网络，适合后续做车型匹配。

### 5.2 提醒缩小范围的结果

出现以下结果时，建议缩小实验范围：

1. Time-CVRP 相比 Baseline CVRP 的核心指标变化低于 5%。
2. 高频子网络 OD 过于稀疏，距离和时间均值不稳定。
3. EVRP 小实例无法稳定生成可行路线。
4. 模型改善主要来自人为参数设定，数据本身没有提供足够支持。
5. 运行时间明显超过4天实验的可控范围。

缩小范围时，优先保留企业数据的物理运输段聚合和 OD 统计。EVRP 可作为方法展示，不扩大到复杂时间窗模型。

## 6. 实验结束后能获得的新信息

实验结束后应得到以下信息：

1. 企业运单数据中真实物理运输段数量。
2. 企业高频运输网络的 OD 距离和时间特征。
3. CVRP 只看距离时的路线优化结果。
4. 加入时间目标后路线是否发生实质变化。
5. 公开 EVRP 数据下新能源路径约束是否可稳定求解。
6. 电池和充电站约束会怎样改变普通 CVRP 路线。
7. 后续是否值得继续做车型匹配、续航校验和补能设施选址。

## 7. 后续扩展方向

如果4天实验结果支持继续，可以按以下顺序扩展：

1. 路线到车型匹配：根据每条路线的里程、载重、体积和时间，判断柴油、纯电、氢能车辆的可行性和成本。
2. 新能源续航校验：加入冬季续航折减、电池衰减和安全余量。
3. 充电站候选点分析：根据 EVRP 路径中的补能需求，反推候选站点价值。
4. 单位减排成本分析：比较路线优化、纯电替换、氢能替换和补能设施投入的元每吨二氧化碳成本。

## 8. 本轮实验边界

本轮实验暂不做以下内容：

1. 不把企业数据和公开 EVRP 数据强行合并。
2. 不直接把车型作为 GA 染色体变量。
3. 不做多仓、两级配送和动态交通。
4. 不把充电站和加氢站数量作为最终结论。
5. 不用公开数据替代企业真实运输数据。

本轮实验的核心价值是建立可信的数据口径和最小模型证据，为后续车型替换和补能设施分析提供基础。
