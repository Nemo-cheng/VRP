# 订单运输网络的 VRP 实验重新设计

## 一、核心研究问题

在订单起终网点、订单重量与体积、历史运输网络和分时段旅行时间已知的条件下，联合优化车辆类型、订单分配、网点访问顺序、运输路径和发车时间，使全部订单在服务期限内完成，并降低车辆数、总行驶距离、空驶距离和运输时间风险。

模型定义为时间依赖异构车队取送货车辆路径问题，简称 TD-HF-PDVRPTW。订单和运输网络只来自 `订单数据.xlsx`。

## 二、为什么采用 VRP 框架

独立最短路径为每个订单单独选路，无法处理以下决策：

- 多个订单是否由同一车辆承运
- 车辆按什么顺序完成取货和送达
- 车辆完成一个任务后如何衔接下一个任务
- 车辆载重和容积如何随访问顺序变化
- 不同车型怎样影响订单合并、车辆数和路线长度
- 分时段旅行时间怎样改变车辆可行路线

VRP 将车辆、订单和路径放入同一个优化问题。它能够检验独立路径优化忽略的车辆共享、容量竞争、时序冲突和空驶衔接。这一差异构成 VRP 与非 VRP 对比的理论依据。

## 三、数据到模型的映射

### 1. 订单请求

对网点连续且时间有序的运单构造取送货请求：

- 取货点：第一段运输的发车网点
- 送达点：最后一段运输的到达网点
- 重量需求：订单重量
- 容积需求：订单体积
- 最早服务时间：历史第一段发车时间
- 回溯服务上限：历史最后一段到达时间
- 历史参考路径：运单实际经过的有效网点序列

回溯服务上限用于构造可比较的实验期限。它不解释为客户承诺时效。

### 2. 运输网络

以匿名网点为节点，以有效历史运输段为有向边。每条边保留：

- 历史距离中位数
- 旅行时间 P50 和 P90
- 月份、星期和发车时段
- 观测次数
- 历史可用车型集合

主模型只使用达到最低观测次数的边。低样本边通过 5、10 和 20 三档阈值进入稳健性实验。

### 3. 候选车辆

车型集合来自 Excel 中实际出现的车型。第一阶段保留样本量充足且名称可以稳定归类的 4.2 米、7.6 米和 9.6 米厢式货车。

订单数据没有额定容量、购置成本、油耗和排放因子。这些量作为显式模型参数进入配置文件，不作为数据事实。正式车辆经济性结论需要可核验参数来源；参数确认前，主结果集中在车辆数、距离、时间、可行率和路线结构。

## 四、联合优化结构

### 第一层：网点路径选择

为每个取送货请求生成 K 条候选路径：

- 距离最短路径
- P50 时间最短路径
- P90 时间最短路径
- 换乘次数最少路径
- 历史有效路径

候选路径必须连续、无环，并满足最大运输段数和最低边观测次数。

### 第二层：车辆路径与调度

在候选路径基础上决定：

- 启用哪些车辆类型
- 每类车辆启用多少辆
- 每个订单分配给哪辆车
- 每辆车的取货和送达顺序
- 同车订单如何合并
- 每个订单选择哪条网点路径
- 车辆任务之间如何空驶衔接
- 各网点的预计到达和离开时间

两个层次联合求解。路径变化会改变车辆可衔接任务和订单送达时间，车型容量会改变订单合并和路线结构。

## 五、决策变量

- `x[k,i,j]`：车辆 `k` 是否从服务节点 `i` 行驶到节点 `j`
- `y[k,r]`：订单 `r` 是否分配给车辆 `k`
- `z[r,p]`：订单 `r` 是否选择候选路径 `p`
- `a[k]`：车辆 `k` 是否启用
- `g[k,t]`：车辆 `k` 是否选择车型 `t`
- `s[i]`：节点 `i` 的服务开始时间
- `l_w[i]`：车辆到达节点 `i` 时的重量载荷
- `l_v[i]`：车辆到达节点 `i` 时的容积载荷

## 六、约束

- 每个订单恰好完成一次取货和一次送达
- 取货发生在对应送达之前
- 车辆访问流保持连续
- 车辆重量和容积不超过车型容量
- 到达和离开时间满足服务期限
- 旅行时间根据出发时段取值
- 同一车辆不能同时执行两个任务
- 订单只能使用连续且无环的候选路径
- 车辆完成前一任务后必须有足够时间到达下一任务起点
- 每辆车满足日最大运行时间
- 未启用车辆不能承运订单

## 七、目标函数

主实验采用分层目标，避免依赖未经核验的成本系数：

1. 最小化未服务和超时订单数
2. 最小化启用车辆数
3. 最小化空驶距离
4. 最小化总行驶距离
5. 最小化 P90 旅行时间风险

车辆成本参数确认后，增加固定车辆成本、单位里程成本、时间成本和排放约束，形成年度总成本目标。

## 八、对比实验

### B0 历史路径基线

保留有效运单的历史网点路径和历史运输时间，用于订单层面的距离与时间比较。

### B1 非 VRP 独立路径优化

每个订单独立选择最短或最快路径，不考虑车辆容量、订单合并、车辆共享和任务衔接。

### B2 贪心车辆调度

按最早服务时间排序，将订单插入当前最早可达且容量可行的车辆。

### B3 静态同构 PDVRPTW

统一车型，使用静态旅行时间，联合优化订单分配和访问顺序。

### B4 静态异构 PDVRPTW

允许多种车型，检验车型选择对车辆数、载荷和路线的影响。

### B5 时间依赖异构 PDVRPTW

使用分时段旅行时间，联合优化车型、订单分配、访问顺序和发车时间。

### B6 路径与车辆联合优化

在 B5 基础上加入订单候选路径选择，作为完整模型。

## 九、核心论证链

- B1 与 B3 对比，证明车辆联合决策的价值
- B3 与 B4 对比，证明异构车型选择的价值
- B4 与 B5 对比，证明分时段旅行时间的价值
- B5 与 B6 对比，证明路径选择和车辆调度联合优化的价值
- 小规模精确解与启发式解对比，证明大规模算法的解质量

每组实验只承担一个机制验证任务。

## 十、评价指标

- 服务订单比例
- 超时订单比例
- 启用车辆总数和车型结构
- 总行驶距离
- 空驶距离和空驶率
- 平均车辆任务数
- 重量和容积利用率
- 平均订单运输时间
- P90 订单运输时间
- 平均换乘次数
- 求解时间
- 小规模实例最优差距

成本和碳排放指标在参数来源确认后加入，不与数据直接观测指标混写。

## 十一、求解框架

### 小规模实例

建立混合整数规划模型，用于 10 至 20 个订单的最优性验证。求解器优先使用 HiGHS。输出目标值、最优性差距和全部约束检查结果。

### 中等规模实例

采用自适应大邻域搜索，包含：

- 随机移除
- 最差成本移除
- 相关订单移除
- 贪心插入
- 遗憾值插入
- 车型变更
- 路径变更
- 跨车辆交换

在小规模实例上与精确解比较，在真实规模实例上比较解质量和求解时间。

## 十二、数据充分性验收

现有审计显示，10,623 个车辆日中只有 283 个包含两个及以上观测任务。历史车辆轨迹不能直接作为完整车队运行记录。

正式建模前执行以下检查：

- 统计每天和每个连通子网络的有效订单数
- 统计可形成 10、20 和 50 单规模实例的日期数量
- 检查取送货时间窗是否具有可行余量
- 检查候选路径覆盖率
- 检查各车型样本量和可归类比例
- 检查任务起点之间的空驶路径覆盖率

若真实日期无法形成足够规模的连通实例，大规模实验采用从真实订单分层抽样构造的标准化基准。此时主张限定为算法实验，企业级车队节省量不作为结论。

## 十三、实施阶段

### 阶段 1：VRP 数据层

- [ ] 构造严格有效的取送货请求表
- [ ] 构造高置信有向运输网络
- [ ] 构造分时段旅行时间矩阵
- [ ] 统计每日连通实例规模
- [ ] 输出数据充分性报告

### 阶段 2：小规模精确模型

- [ ] 生成 10 至 20 单实例
- [ ] 实现取送货、容量、时间和车辆流约束
- [ ] 实现车型选择
- [ ] 实现候选路径选择
- [ ] 输出逐约束可行性检查

### 阶段 3：基线与消融

- [ ] 实现 B0 至 B6
- [ ] 统一输入实例和评价指标
- [ ] 分别验证车型、时段和路径联合机制

### 阶段 4：规模扩展

- [ ] 实现自适应大邻域搜索
- [ ] 与小规模精确解比较
- [ ] 扩展到数据能够支持的最大真实实例
- [ ] 进行参数和样本阈值稳健性实验

## 十四、当前决策

本分支先完成阶段 1。数据充分性报告通过后再确定正式实例规模、车辆参数口径和求解算法实现。旧实验文件暂时保留，待新模型通过小规模可行性验证后统一清理。
