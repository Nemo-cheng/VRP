# VRP框架与车辆路线联合优化设计说明

## 一、研究目标

实验围绕两个问题展开：

1. 订单逐个选择最短路径后，为什么还需要 VRP 框架。
2. 在 VRP 框架内，怎样把车辆选择和路线规划做成真正的联合决策。

第一部分证明联合调度的必要性，第二部分证明车辆和路线相互影响时能够形成更完整的运输方案。两部分使用同一批订单、同一张运输网络和同一套评价指标。

## 二、第一方面：突出VRP框架的作用

### 1. 设计逻辑

先为每个订单独立选择距离最短或时间最短的运输路径，形成非 VRP 方案。这个方案只回答订单应该经过哪些网点，不处理车辆之间的协调。

再把同一批订单放入 VRP 框架。模型同时决定：

- 哪些订单由同一辆车承运
- 每辆车先取哪个订单、后送哪个订单
- 一辆车完成当前任务后衔接哪个任务
- 车辆何时发车
- 车辆在任务之间需要多少空驶

两组方案的订单和网络完全相同。差异只来自车辆层面的联合决策，因此可以直接观察 VRP 框架带来的变化。

### 2. 为什么设计这一步

订单独立最短路径可能让大量订单同时使用同一条边，也可能为相邻任务分别安排车辆。每个订单单独看都合理，组合后会出现车辆数量过多、任务时间冲突、车辆无法及时到达下一任务起点等问题。

VRP 的价值体现在全局协调。它将订单路径转换为可由车辆连续执行的运输计划，统一处理车辆共享、访问顺序、时间衔接和空驶移动。

### 3. 解决的问题

这一部分主要解决四个问题：

- 订单独立选路无法形成完整车辆计划
- 同一车辆可能被重复安排在时间重叠的任务上
- 相邻订单缺少车辆衔接关系
- 路径距离最短不代表所需车辆数和空驶距离最少

### 4. 对比内容

非 VRP 方案输出：

- 每个订单的独立路径
- 订单路径总距离
- 订单预计运输时间
- 路径换乘次数

VRP 方案增加输出：

- 每辆车承担的订单
- 每辆车访问网点的顺序
- 车辆任务衔接和空驶路线
- 车辆发车、到达和等待时间
- 总车辆数、总行驶距离和空驶距离

### 5. 进入下一步的标准

满足以下条件后，才进入车辆与路线联合优化：

- 选定实验实例中的订单请求有效率达到 100%
- 每个订单都有连续且无环的候选路径
- 历史参考路径在其回溯服务期限内可行率达到 100%
- 非 VRP 和 VRP 使用完全相同的订单、网点和旅行时间输入
- VRP 方案覆盖全部订单，不允许遗漏订单
- 车辆路线的时间重叠、网点断裂和重复服务均为 0
- 至少形成 20 个相互独立的实验实例，避免结论来自单个日期
- 每个实例至少包含 10 个订单，并且存在订单共享车辆或任务连续执行的空间

如果真实日期无法形成 20 个满足条件的实例，需要先调整实例范围或构造规则，不能直接进入性能对比。

## 三、第二方面：车辆与路线联合优化

### 1. 设计逻辑

车辆选择和路线规划同时决定。模型不能先固定车辆再找路线，也不能先固定路线再选择车辆。

一条较短路径可能集中大量订单，需要更多车辆或更大车型。另一条路径距离稍长，但能够连接更多后续任务，减少空驶和车辆数。大车型能够合并更多订单，小车型在低需求路径上更灵活。车辆类型变化会改变可合并订单和可执行路线，路线变化也会改变车辆的时间和载荷状态。

联合优化需要同时回答：

- 使用哪些车型
- 每类车型需要多少辆
- 哪些订单可以合并
- 每辆车访问哪些网点
- 订单选择哪条网点路径
- 车辆怎样从一个任务衔接到下一个任务
- 什么时候发车能够避开高旅行时间时段

### 2. 为什么设计这一步

车辆和路线分开优化会产生局部最优。固定车型后优化路线，无法判断换用其他车型是否可以减少车辆数。固定路线后选择车型，无法利用路线调整改善订单组合和任务衔接。

联合设计把装载、路径、时段和车辆连续运行放在同一个方案中，最终输出可以逐车执行的运输计划。

### 3. 解决的问题

这一部分重点解决：

- 车型与订单需求不匹配
- 大量小订单分散在不同车辆上
- 车辆完成送达后无法衔接附近任务
- 路径只考虑载货距离，忽略车辆空驶
- 静态旅行时间造成发车时间和到达时间判断失真
- 局部最短路径导致全车队总距离增加

### 4. 优化层次

车辆与路线联合优化分为四层，每增加一层只引入一种新决策：

1. 固定单一车型，只优化订单分配和车辆访问顺序。
2. 增加车型选择，比较车辆数量和装载结构。
3. 增加分时段旅行时间，调整发车和任务衔接。
4. 增加候选路径选择，同时优化载货路径和空驶路径。

这种递进设计能够判断最终改进来自车型、时段还是路径选择，避免只展示一个无法解释的综合结果。

### 5. 车辆参数口径

车型名称和历史使用范围来自 `订单数据.xlsx`。车辆额定载重、额定容积、固定成本和单位里程成本需要作为配置参数记录来源。

参数确认前，实验以车辆数、距离、时间、订单覆盖率和约束可行性为主。成本和排放进入后续情景实验，不承担第一阶段的核心证明任务。

### 6. 进入正式主实验的标准

小规模联合模型满足以下条件后，才能扩展到完整对比实验：

- 全部订单完成取货和送达，订单覆盖率为 100%
- 所有车辆的重量和容积约束违规数为 0
- 所有取货先于对应送达
- 所有车辆任务在时间上连续且无重叠
- 所有车辆在相邻任务之间存在可达路径
- 所有推荐载货路径连续、无环且满足边样本阈值
- 每辆车的逐段载荷、到达时间和离开时间能够完整复算
- 固定输入重复运行时得到一致的可行性结论
- 至少在 10 单和 20 单实例上完成逐约束核验
- VRP、车型选择、分时段和路径选择四个实验层次均能独立开关

### 7. 进入规模实验的标准

正式主实验完成后，满足以下条件再扩展实例规模：

- 小规模方案全部通过可行性检查
- 不同方案使用相同实例和相同参数
- 每项新增机制都有单独对照组
- VRP 方案的订单服务率不低于非 VRP 方案
- 联合方案至少改善车辆数、空驶距离、总距离或 P90 运输时间中的一项
- 其他核心指标的变化能够由车型、时段或路径机制解释
- 数据中存在足够数量的同规模独立实例

不预先要求所有指标同时改善。车辆减少可能增加单车运行时间，距离降低也可能增加等待时间。最终结果需要展示这些权衡，并说明推荐方案采用的优先级。

## 四、两个方面的关系

第一方面回答是否需要 VRP。它通过同一批订单上的独立路径方案和联合车辆方案对比，证明车辆协调带来的价值。

第二方面回答 VRP 还能优化到什么程度。它逐步加入车型、分时段旅行时间和候选路径，使车辆计划从订单分配扩展为完整的车辆与路线联合方案。

实验顺序固定为：

1. 验证订单和候选路径数据。
2. 完成非 VRP 与基础 VRP 对比。
3. 加入车型选择。
4. 加入分时段旅行时间。
5. 加入路径与车辆联合选择。
6. 通过小规模约束检查后扩展正式实验。

每一步达到对应标准后再进入下一步。任何一步出现订单遗漏、容量违规、时间冲突或路径断裂，都先修正模型和数据，不使用目标值改善掩盖不可行方案。
