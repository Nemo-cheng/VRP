# 仅基于订单数据.xlsx的网点路径与发车时段优化

## 一、研究问题

利用 `订单数据.xlsx` 中运单经过的运输网点、各段发车与到达时间、运输距离和订单重量，优化网点级候选路径与发车时段。目标是在可验证的数据范围内减少运输距离、运输时间、换乘次数和时间波动，并降低订单重量乘运输距离形成的运输周转量。

本实验不再研究车辆真实装载率、车次合并、车队规模和油电车替换。Excel 记录的是部分订单经过车辆的轨迹，不是每辆车的完整载货清单。

## 二、数据能支持什么

### 可以直接观测

- 每个运单经过的运输段
- 运输段的起点网点、终点网点、距离、发车时间和到达时间
- 运单重量、体积、件数和妥投时间
- 历史车型和匿名车辆标识
- 同一运输边在不同日期和时段的旅行时间分布

### 可以构造的优化指标

- 路径总距离
- 路径总历时
- 干线行驶时间
- 网点中转等待时间
- 换乘次数
- 旅行时间 P50、P90 和波动程度
- 订单重量乘路径距离的运输周转量

运输周转量只能作为能源消耗和碳排放的代理指标。没有油耗、电耗和排放因子时，不能输出真实碳排放量。

### 数据不能识别

- 车辆完整载货量和真实装载率
- 车辆额定容量
- 车次是否能够合并
- 车辆购置、运营和维护成本
- 燃油消耗、电耗和真实碳排放
- 电池续航、SOC 和充电过程
- 客户级道路访问顺序

## 三、可优化性审计结果

审计脚本为 `experiment/audit_excel_optimization_scope.py`，结果写入 `results/company_transport/optimization_scope_audit.json`。

- 12,098 个运单中，11,465 个包含多个运输段
- 只有 6,486 个运单同时满足网点连续和时间有序，占 53.6%
- 相邻运输段的网点连续率为 68.4%
- 相邻运输段的时间非重叠率为 86.6%
- 有效路径覆盖 2,093 个首尾网点对
- 95 个网点对存在多条有效历史路径，涉及 661 个运单
- 只有 16 个网点对拥有至少两条重复观测的替代路径
- 3,183 条有向运输边中，337 条至少观测 10 次，161 条至少观测 20 次
- 4,306 个运输边时段单元中，316 个至少观测 10 次

这些结果决定了实验边界。完整历史路径比较的样本较少，只能作为验证性子实验。高频运输边的分时段性能估计更可靠，应成为主实验的数据基础。

## 四、优化对象

### 1. 有效运单路径

按运单号和发车时间排序运输段。只有整条路径满足以下条件时才进入路径实验：

- 上一段到达网点等于下一段发车网点
- 上一段到达时间不晚于下一段发车时间
- 每段距离为正
- 每段运输时长为正且不超过异常阈值

### 2. 高频运输边

按起点网点和终点网点聚合历史运输段。主模型只使用训练期至少观测 10 次的边。分时段模型只使用同一边同时段至少观测 5 次的单元，低于阈值时回退到该边的全时段统计。

### 3. 发车时段

初始划分为夜间、早高峰、日间、晚高峰和晚间。最终边界需要根据发车时刻分布和每个单元的样本量调整，不能直接把城市道路高峰定义套在全部干线网点上。

## 五、目标函数和约束

### 目标

采用多目标优化，不把不同单位直接相加后宣称唯一最优：

- 最小化路径总距离
- 最小化预计总历时或 P90 总历时
- 最小化换乘次数
- 最小化订单运输周转量

先输出 Pareto 候选方案，再用明确权重或约束选择推荐方案。

### 约束

- 路径起点和终点必须与运单首尾网点一致
- 路径必须连续且不能包含环
- 每条边必须在训练数据中达到最低观测次数
- 路径运输段数不超过对应网点对历史分布的上限
- 使用分时段旅行时间时，下一段发车不得早于上一段预计到达
- 无法获得可靠候选路径的网点对保留历史路径，不强行优化

## 六、实验设计

### A 数据审计，已完成

- [x] 订单与运单关联
- [x] 物理运输段构造
- [x] 网点连续性和时间顺序检查
- [x] 车辆载货清单完整性检查
- [x] 历史替代路径和高频运输边统计

### B 有效路径数据层

- [ ] 生成有效运单路径明细
- [ ] 分解行驶时间和中转等待时间
- [ ] 生成有向边和边时段统计
- [ ] 对网点和路径标识做匿名化

### C 时间外验证设计

- [ ] 按日期划分训练期和测试期，禁止随机拆分造成时间泄漏
- [ ] 在训练期估计距离、旅行时间 P50 和 P90
- [ ] 在测试期评估时间预测误差和路径覆盖率
- [ ] 记录因边样本不足而无法优化的运单比例

### D 历史替代路径实验

- [ ] 对存在多条有效历史路径的网点对建立候选集
- [ ] 比较距离、总历时、等待时间和换乘次数
- [ ] 对 16 个具有重复替代路径的网点对做重点验证
- [ ] 输出 Pareto 路径，不扩大解释到全网络

### E 高频边发车时段实验

- [ ] 比较同一运输边不同时段的 P50 和 P90 旅行时间
- [ ] 只对样本量达标的边推荐时段
- [ ] 检查月份和星期差异，避免把季节变化误判为时段效应
- [ ] 输出时段调整后的预计时间变化和适用边范围

### F 网点级候选路径优化

- [ ] 使用高置信运输边建立有向图
- [ ] 生成距离最短、时间最短和换乘最少的无环候选路径
- [ ] 限制路径段数和低样本边
- [ ] 区分历史观测路径和图上新组合路径
- [ ] 对新组合路径只报告模型候选，不声称具备实际班次可行性

### G 稳健性实验

- [ ] 最低边观测次数取 5、10 和 20
- [ ] 旅行时间使用 P50 和 P90
- [ ] 比较工作日、周末和月份
- [ ] 比较不同最大换乘次数
- [ ] 比较距离优先、时间优先和换乘优先方案

## 七、最终交付

- 可重构运单路径及质量审计
- 高频运输边的分时段距离和时间统计
- 符合数据证据的历史替代路径推荐
- 网点级候选路径及每段预计时间
- 距离、时间、换乘次数和运输周转量的变化
- 训练期与测试期的覆盖率和误差
- 无法优化的网点对及原因

最终结果是网点级运输路径和时段方案。结果不包含车辆替换、真实成本、真实碳排放和客户级道路导航。

## 八、下一步

执行阶段 B，建立严格的有效路径、运输边和边时段三张标准化数据表。完成后先检查覆盖率，再决定主模型使用历史候选路径还是高置信运输图。
