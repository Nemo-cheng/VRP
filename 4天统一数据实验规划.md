# 4天统一数据实验规划

## 1. 实验目标

实验使用 `jd200_1` 同时完成三组路线优化。客户、仓库、充电站、取送货需求、车辆容量、距离矩阵和行驶时间矩阵保持一致。实验只逐步增加时间窗、电池续航和充电约束。

数据位置：

```text
external_data/EVRP-TW-SPD-HMA/data/jd_instances/jd200_1.txt
```

该实例包含 1 个仓库、200 个客户和 100 个充电站。`订单数据.xlsx` 用于企业运输现状统计，不进入本轮路线求解。

## 2. 对照模型

| 模型 | 同步取送货 | 时间窗 | 电池与充电 |
|---|---|---|---|
| VRP SPD | 启用 | 放宽 | 放宽 |
| VRP TW SPD | 启用 | 启用 | 放宽 |
| EVRP TW SPD | 启用 | 启用 | 启用 |

三个模型使用相同随机种子、种群规模、搜索算子和运行时间。

## 3. 核心指标

| 指标 | 用途 |
|---|---|
| 总成本 | 比较车辆固定成本与运输成本 |
| 车辆数 | 观察约束增加后的运力需求 |
| 总距离 | 衡量绕行和路线重组 |
| 总行驶时间 | 衡量运输组织变化 |
| 等待时间 | 衡量时间窗造成的空耗 |
| 充电时间 | 衡量补能耗时 |
| 充电站访问次数 | 衡量补能需求 |
| 总能耗 | 衡量新能源运输规模 |
| 最低剩余可行驶距离 | 判断续航约束紧张程度 |
| 模型可行性 | 检查容量、时间窗和电池约束 |

## 4. 执行安排

### Day 1

解析 `jd200_1`，生成三组约束版本，使用 Docker 编译官方 HMA 求解器，完成 30 秒快速检查。

### Day 2

三个模型分别运行 10 次，每次使用不同随机种子，每次运行上限为 1800 秒。

### Day 3

汇总最优值、均值和标准差，计算路线、时间、等待、充电和能耗指标，核查解的可行性。

### Day 4

绘制模型对比图和典型路线图，解释时间窗与新能源约束的边际影响，形成报告结论。

## 5. 运行方式

快速检查：

```powershell
uv run python experiment/run.py --skip-build --time-limit 30
```

正式单次实验：

```powershell
uv run python experiment/run.py --time-limit 1800
```

结果写入：

```text
results/raw
results/logs
results/unified_experiment_metrics.csv
results/unified_experiment_metrics.json
```

## 6. 结论口径

`VRP SPD` 到 `VRP TW SPD` 的变化刻画时间窗影响，`VRP TW SPD` 到 `EVRP TW SPD` 的变化刻画续航与补能影响。三组结果来自同一数据环境，可以直接比较。
